# Rules and Constraints

- **Doctor Admin & Secretary Dashboard Frozen**: The doctor admin panel and secretary interface (`/src/components/Dashboard.tsx`) are strictly finalized. Do NOT make any edits or modifications to `Dashboard.tsx` or its associated permission logic in future requests unless the user explicitly requests changes to it.
